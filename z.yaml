tasks:
  symlink: mkdir -p ~/.vmodules/zakuro9715 && ln -s $(pwd) ~/.vmodules/zakuro9715/vna
  helloworld: v run examples/hello_world
  build:
    tasks:
      examples: |
        for f in examples/*
        do
          v $f
        done
